[pytest]
markers =
    unit: Fast unit tests that mock all external dependencies.
    integration: Integration tests that require running Docker services.
    slow: Slow tests requiring models, transcription, or LLM interaction.
